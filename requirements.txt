requests>=2.31.0   # still used directly by the legacy isDrupal.py script
curl_cffi>=0.15.0  # TLS/HTTP2 browser impersonation for isdrupal.core (Cloudflare fingerprinting)
beautifulsoup4>=4.12.0
lxml>=4.9.0        # optional: faster HTML parsing; falls back to html.parser if absent
tqdm>=4.66.0       # optional: CLI batch progress bar

# ── Web app (app.py) ──
flask>=3.0.0
gunicorn>=21.2.0   # production: run with --workers 1 (in-memory job registry)
# flask-limiter>=3.5.0  # optional: only if you enable rate_limit() in isdrupal/security.py

# playwright>=1.40.0  # optional: needed for --browser-fallback
#                       pip install playwright && playwright install chromium
